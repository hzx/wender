Prerequisites
sudo gem install jasmine-headless-webkit

Web server, realtime async cgi and wsgi versions.
For run, monitor web server components use supervisor or puppet.

Need to generate only config.py, handlers.py, urls.py

wender_coffee - frontend library, stay behind mutant web app.
